[pytest]
# Run from python/: `python -m pytest`. tests/conftest.py puts the sfmap package
# (and this dir) on sys.path, so individual test files don't need a bootstrap.
testpaths = tests
python_files = test_*.py
python_functions = test_*
